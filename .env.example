# Database (lokal ohne Eintrag = SQLite, sonst Postgres/Neon):
# – Einfaches Setup: DATABASE_URL weglassen → es wird SQLite (./payload.db) genutzt
# – Neon: DATABASE_URL mit pooled connection string aus dem Dashboard
# – Lokal Postgres: POSTGRES_URL oder DATABASE_URL
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
# POSTGRES_URL=postgresql://127.0.0.1:5432/your-database-name
# SQLITE_URL=file:./payload.db
# SQLite: Schema-Push deaktivieren, wenn Tabellen/Spalten schon per "pnpm run fix:sqlite-schema" angelegt wurden (vermeidet "duplicate column name")
# PAYLOAD_SKIP_PUSH=true

# Payload: used to encrypt JWT tokens (use a long random string, e.g. openssl rand -hex 32)
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Public URL of the app, no trailing slash (Vercel sets this automatically in production)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Vercel Blob storage for Media uploads (create in Vercel project → Storage)
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# Optional: secret for cron job auth; in Production auch für /api/revalidate (?secret=...) nötig
# CRON_SECRET=YOUR_CRON_SECRET_HERE
# REVALIDATE_SECRET=YOUR_REVALIDATE_SECRET_HERE

# Live Preview in Admin: min. 12 characters (e.g. openssl rand -hex 12)
# PREVIEW_SECRET=YOUR_PREVIEW_SECRET_MIN_12_CHARS
